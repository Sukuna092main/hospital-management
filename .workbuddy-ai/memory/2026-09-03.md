# 2026-09-03

- Khởi tạo rồi hiệu chỉnh cấu trúc backend theo bản `AGENTS.md` đầy đủ: Express + TypeScript; 4 service Postgres dùng `prisma/`, `src/{routes,controllers,services,schemas,middlewares,lib,types}`, `test/`; Clinical Service dùng Mongoose với `src/models` thay Prisma. Đã loại bỏ các thư mục `src/db` tạo nhầm trước đó.
- Cập nhật yêu cầu của giảng viên: tách riêng frontend theo nền tảng, không tách cứng theo vai trò. `mobile-app` dùng Expo cho iOS/Android, `web-app` dùng Next.js cho web/tablet/desktop; cả hai hỗ trợ bệnh nhân và nhân viên qua RBAC/role-based navigation. Không dùng Expo Web thay cho Next.js web app.
